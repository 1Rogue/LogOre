main: com.rogue.logore.LogOre
name: ${project.name}
prefix: LogOre
description: Allows for simple tracking of ore mining without spamming the chat
version: ${project.version}
author: 1Rogue
website: https://1rogue.com/
commands:
  orelog:
    description: Information about LogOre
permissions:
  logore.*:
    default: op
    children:
    - logore.reload
    - logore.exempt